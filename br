local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")

local player = Players.LocalPlayer

local InfiniteJumpEnabled = false
local MinSpeed = 50
local MaxSpeed = 500
local CurrentSpeed = 50
local FPSBoostEnabled = false

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
ScreenGui.Name = "MovementGui"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local Main = Instance.new("Frame")
Main.Size = UDim2.new(0, 360, 0, 560)
Main.Position = UDim2.new(0.5, -180, 0.5, -280)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.BackgroundTransparency = 0.05
Main.Active = true
Main.Parent = ScreenGui

local Shadow = Instance.new("ImageLabel")
Shadow.Size = UDim2.new(1, 20, 1, 20)
Shadow.Position = UDim2.new(0, -10, 0, -10)
Shadow.BackgroundTransparency = 1
Shadow.Image = "rbxassetid://1316045217"
Shadow.ImageColor3 = Color3.new(0, 0, 0)
Shadow.ImageTransparency = 0.6
Shadow.ScaleType = Enum.ScaleType.Slice
Shadow.SliceCenter = Rect.new(10, 10, 118, 118)
Shadow.Parent = Main

local corner = Instance.new("UICorner", Main)
corner.CornerRadius = UDim.new(0, 20)

local stroke = Instance.new("UIStroke", Main)
stroke.Thickness = 4
stroke.Transparency = 0.2

local hue = 0
RunService.RenderStepped:Connect(function(delta)
    hue = hue + delta * 0.15
    if hue > 1 then hue = 0 end
    stroke.Color = Color3.fromHSV(hue, 1, 1)
end)

local draggingFrame = false
local dragStart
local startPos
local draggingSlider = false

Main.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 and not draggingSlider then
        draggingFrame = true
        dragStart = input.Position
        startPos = Main.Position
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingFrame = false
        draggingSlider = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if draggingFrame and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        Main.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1
Title.Text = "Zephyx Hub"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextScaled = true
Title.Font = Enum.Font.FredokaOne
Title.TextStrokeTransparency = 0.5

local JumpButton = Instance.new("TextButton", Main)
JumpButton.Size = UDim2.new(0.8, 0, 0, 45)
JumpButton.Position = UDim2.new(0.1, 0, 0, 60)
JumpButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
JumpButton.Text = "Infinite Jump: OFF"
JumpButton.TextColor3 = Color3.new(1, 1, 1)
JumpButton.Font = Enum.Font.GothamBold
JumpButton.TextScaled = true
local jumpCorner = Instance.new("UICorner", JumpButton)
jumpCorner.CornerRadius = UDim.new(0, 12)

JumpButton.MouseEnter:Connect(function()
    TweenService:Create(JumpButton, TweenInfo.new(0.2), {BackgroundColor3 = JumpButton.BackgroundColor3:Lerp(Color3.new(1,1,1), 0.2)}):Play()
end)
JumpButton.MouseLeave:Connect(function()
    TweenService:Create(JumpButton, TweenInfo.new(0.2), {BackgroundColor3 = InfiniteJumpEnabled and Color3.fromRGB(0,170,0) or Color3.fromRGB(170,0,0)}):Play()
end)

JumpButton.MouseButton1Click:Connect(function()
    InfiniteJumpEnabled = not InfiniteJumpEnabled
    if InfiniteJumpEnabled then
        JumpButton.Text = "Infinite Jump: ON"
        JumpButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        JumpButton.Text = "Infinite Jump: OFF"
        JumpButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
    end
end)

local SpeedLabel = Instance.new("TextLabel", Main)
SpeedLabel.Size = UDim2.new(1, 0, 0, 30)
SpeedLabel.Position = UDim2.new(0, 0, 0, 115)
SpeedLabel.BackgroundTransparency = 1
SpeedLabel.Text = "Speed: " .. CurrentSpeed
SpeedLabel.TextColor3 = Color3.new(1, 1, 1)
SpeedLabel.Font = Enum.Font.GothamBold
SpeedLabel.TextScaled = true

local SliderBar = Instance.new("Frame", Main)
SliderBar.Size = UDim2.new(0.8, 0, 0, 8)
SliderBar.Position = UDim2.new(0.1, 0, 0, 155)
SliderBar.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
local barCorner = Instance.new("UICorner", SliderBar)
barCorner.CornerRadius = UDim.new(1, 0)

local SliderButton = Instance.new("Frame", SliderBar)
SliderButton.Size = UDim2.new(0, 22, 0, 22)
SliderButton.Position = UDim2.new(0, -11, -0.7, 0)
SliderButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
local thumbCorner = Instance.new("UICorner", SliderButton)
thumbCorner.CornerRadius = UDim.new(1, 0)

SliderButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        draggingSlider = true
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if draggingSlider and input.UserInputType == Enum.UserInputType.MouseMovement then
        local relativeX = (input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X
        relativeX = math.max(0, math.min(1, relativeX))
        SliderButton.Position = UDim2.new(relativeX, -11, -0.7, 0)
        CurrentSpeed = math.floor(MinSpeed + (MaxSpeed - MinSpeed) * relativeX)
        SpeedLabel.Text = "Speed: " .. CurrentSpeed
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = CurrentSpeed
            end
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if InfiniteJumpEnabled then
        local character = player.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
            end
        end
    end
end)

local function updateSliderFromSpeed(speed)
    local relativeX = (speed - MinSpeed) / (MaxSpeed - MinSpeed)
    relativeX = math.max(0, math.min(1, relativeX))
    SliderButton.Position = UDim2.new(relativeX, -11, -0.7, 0)
    SpeedLabel.Text = "Speed: " .. speed
end

player.CharacterAdded:Connect(function(char)
    local hum = char:WaitForChild("Humanoid")
    hum.WalkSpeed = CurrentSpeed
    updateSliderFromSpeed(CurrentSpeed)
end)

local celestialHeader = Instance.new("TextLabel", Main)
celestialHeader.Size = UDim2.new(1, 0, 0, 25)
celestialHeader.Position = UDim2.new(0, 0, 0, 190)
celestialHeader.BackgroundTransparency = 1
celestialHeader.Text = "✨ Celestial ✨"
celestialHeader.TextColor3 = Color3.fromRGB(200, 200, 255)
celestialHeader.Font = Enum.Font.GothamBold
celestialHeader.TextSize = 18

local TPButtons = {
    { -45, 165, -2260, "tpcelestial1", 0.05, 220 },
    { 26, 165, -2352, "tpcelestial2", 0.55, 220 },
    { -32, 165, -2503, "tpcelestial3", 0.05, 260 },
    { 20, 165, -2664, "tpcelestial4", 0.55, 260 }
}

for _, v in pairs(TPButtons) do
    local btn = Instance.new("TextButton", Main)
    btn.Size = UDim2.new(0.38, 0, 0, 35)
    btn.Position = UDim2.new(v[5], 0, 0, v[6])
    btn.BackgroundColor3 = Color3.fromRGB(120, 0, 200)
    btn.Text = v[4]
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextScaled = true
    local btnCorner = Instance.new("UICorner", btn)
    btnCorner.CornerRadius = UDim.new(0, 12)

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(160, 80, 255)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(120, 0, 200)}):Play()
    end)

    btn.MouseButton1Click:Connect(function()
        local character = player.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new(v[1], v[2], v[3])
        end
    end)
end

local godlyHeader = Instance.new("TextLabel", Main)
godlyHeader.Size = UDim2.new(1, 0, 0, 25)
godlyHeader.Position = UDim2.new(0, 0, 0, 310)
godlyHeader.BackgroundTransparency = 1
godlyHeader.Text = "👑 Godly 👑"
godlyHeader.TextColor3 = Color3.fromRGB(255, 255, 150)
godlyHeader.Font = Enum.Font.GothamBold
godlyHeader.TextSize = 18

local GodlyButtons = {
    { -31, 382, -3023, "tpgodly1", 0.05, 340 },
    { 11, 382, -3253, "tpgodly2", 0.55, 340 },
    { -15, 440, -3503, "tpgodly3", 0.3, 385 }
}

for _, v in pairs(GodlyButtons) do
    local btn = Instance.new("TextButton", Main)
    btn.Size = UDim2.new(0.38, 0, 0, 35)
    btn.Position = UDim2.new(v[5], 0, 0, v[6])
    btn.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
    btn.Text = v[4]
    btn.TextColor3 = Color3.new(0, 0, 0)
    btn.Font = Enum.Font.GothamBold
    btn.TextScaled = true
    local btnCorner = Instance.new("UICorner", btn)
    btnCorner.CornerRadius = UDim.new(0, 12)

    btn.MouseEnter:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 240, 100)}):Play()
    end)
    btn.MouseLeave:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(255, 200, 0)}):Play()
    end)

    btn.MouseButton1Click:Connect(function()
        local character = player.Character
        if character and character:FindFirstChild("HumanoidRootPart") then
            character.HumanoidRootPart.CFrame = CFrame.new(v[1], v[2], v[3])
        end
    end)
end

local TPBaseButton = Instance.new("TextButton", Main)
TPBaseButton.Size = UDim2.new(0.8, 0, 0, 45)
TPBaseButton.Position = UDim2.new(0.1, 0, 0, 440)
TPBaseButton.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
TPBaseButton.Text = "🏠 TP to Base"
TPBaseButton.TextColor3 = Color3.new(1, 1, 1)
TPBaseButton.Font = Enum.Font.GothamBold
TPBaseButton.TextScaled = true
local baseCorner = Instance.new("UICorner", TPBaseButton)
baseCorner.CornerRadius = UDim.new(0, 12)

TPBaseButton.MouseEnter:Connect(function()
    TweenService:Create(TPBaseButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(80, 180, 255)}):Play()
end)
TPBaseButton.MouseLeave:Connect(function()
    TweenService:Create(TPBaseButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 120, 255)}):Play()
end)

TPBaseButton.MouseButton1Click:Connect(function()
    local character = player.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        character.HumanoidRootPart.CFrame = CFrame.new(98, 4, 36)
    end
end)

local function optimizeObject(v)
    if v:IsA("BasePart") then
        v.CastShadow = false
        v.Material = Enum.Material.Plastic
        v.Reflectance = 0
    elseif v:IsA("Decal") or v:IsA("Texture") then
        v.Transparency = 1
    elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then
        v.Enabled = false
    elseif v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
        v.Enabled = false
    end
end

local optimizerConnection

local function applyAdvancedOptimizer()
    if optimizerConnection then
        optimizerConnection:Disconnect()
        optimizerConnection = nil
    end
    for _, v in ipairs(Workspace:GetDescendants()) do
        optimizeObject(v)
    end
    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.Brightness = 1
        Lighting.FogEnd = 100000
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    end)
    optimizerConnection = Workspace.DescendantAdded:Connect(function(v)
        if FPSBoostEnabled then
            optimizeObject(v)
        end
    end)
end

local function disableOptimizer()
    if optimizerConnection then
        optimizerConnection:Disconnect()
        optimizerConnection = nil
    end
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
        Lighting.GlobalShadows = true
    end)
end

local FPSButton = Instance.new("TextButton")
FPSButton.Parent = Main
FPSButton.Size = UDim2.new(0.8, 0, 0, 45)
FPSButton.Position = UDim2.new(0.1, 0, 0, 500)
FPSButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
FPSButton.Text = "⚡ FPS Booster (OFF)"
FPSButton.TextColor3 = Color3.new(1, 1, 1)
FPSButton.Font = Enum.Font.GothamBold
FPSButton.TextScaled = true

Instance.new("UICorner", FPSButton).CornerRadius = UDim.new(0, 12)

FPSButton.MouseButton1Click:Connect(function()
    FPSBoostEnabled = not FPSBoostEnabled
    if FPSBoostEnabled then
        applyAdvancedOptimizer()
        FPSButton.Text = "⚡ FPS Booster (ON)"
        FPSButton.BackgroundColor3 = Color3.fromRGB(0, 170, 0)
    else
        disableOptimizer()
        FPSButton.Text = "⚡ FPS Booster (OFF)"
        FPSButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
    end
end)

if player.Character then
    local hum = player.Character:FindFirstChildOfClass("Humanoid")
    if hum then
        CurrentSpeed = hum.WalkSpeed
        updateSliderFromSpeed(CurrentSpeed)
    end
end
